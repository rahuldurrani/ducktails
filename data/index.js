const recipeData = require("./recipe");
const userData = require("./user");
const categoryData = require("./category");

module.exports = {
    users: userData,
    recipe: recipeData,
    user: userData
};